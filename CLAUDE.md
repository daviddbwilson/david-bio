# CLAUDE.md - david.bio

## Content Sync Requirement

**CRITICAL:** When making ANY changes to website copy in `index.html`, you MUST also update `website copy.md` to keep it in sync. This file serves as the canonical plain-text version of all website content.

After editing `index.html`:
1. Extract the updated copy from the HTML
2. Update `website copy.md` with the matching content
3. Verify both files reflect the same text

This ensures the markdown file can be used as a reference for AI tools, content reviews, and future editing without parsing HTML.
